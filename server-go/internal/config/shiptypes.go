package config

// Ship types are now stored in the database (ship_types table).
// This file is kept for backwards compatibility but all type definitions
// and validation are now handled via DB lookups.
