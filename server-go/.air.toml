root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o bin/server ./cmd/server"
  bin = "./bin/server"
  full_bin = "./bin/server"
  include_ext = ["go", "sh"]
  exclude_dir = ["tmp", "vendor", "data"]
  delay = 1000
  stop_on_error = true

[log]
  time = true

[misc]
  clean_on_exit = true
